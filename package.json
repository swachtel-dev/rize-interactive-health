{
  "name": "rize-interactive-health",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Opens every externally-hosted Rize course page in a real browser and reports whether a student would see it working.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "status": "node scripts/render_status.mjs",
    "all": "node scripts/check.mjs; node scripts/render_status.mjs"
  },
  "dependencies": {
    "playwright": "1.49.1"
  }
}
